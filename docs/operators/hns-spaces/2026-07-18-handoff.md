# HNS/Spaces Bring-up Handoff — 2026-07-18

Status captured at approximately 12:34 UTC. This file records runtime state
and restart order; linked specifications remain authoritative for behavior.

## Outcome so far

- Multi-namespace handles, per-namespace policy, claim-time eligibility,
  payment reservations, abuse controls, reserved roots, and perpetual-v0
  lifecycle decisions are shipped.
- Production API was observed at `1050e96a9f5d353d1fd83a6d69561c1b1ddc9b0d`
  (`/__version`, built `2026-07-18T12:22:13Z`). Recheck on resume; this is
  expected to advance independently.
- ns1 is `94.103.168.161`; ns2 is `81.15.150.159`.
- Primary and secondary PowerDNS are running, pinned, and drift-free. Both
  contain zero real zones. Signed NOTIFY/TSIG AXFR was proven with a disposable
  zone and then cleaned up.
- Encrypted B2 backup and restore are proven against real infrastructure:
  archive plus checksum receive 30-day COMPLIANCE retention, the Infisical-
  escrowed age identity restored the archive, and PowerDNS SQLite passed
  integrity verification.
- Backup success heartbeats are accepted by production. Explicit failure
  alerts immediately; missing success for 36 hours covers a dead timer/host.
- Do not deploy protocol `subsd` or enable `spaces_subspace` issuance. That
  path remains deliberately blocked.

## Exact edge state

| Host/root | Role | Core commit | State |
| --- | --- | --- | --- |
| ns1 `/srv/pirate-hns` | state backup | `515316a4bc7bd5ea5d3a1f1ae8f2a51c87dab443` | timer active, drift none |
| ns1 `/srv/pirate-hns-authdns` | primary DNS | `44c23421cee7452e78c01afd8fda33f2c0d9175d` | running, zero zones, drift none |
| ns1 `/srv/pirate-hns-observer` | keyless HNS observer | `44c23421cee7452e78c01afd8fda33f2c0d9175d` | IBD, drift none |
| ns1 `/srv/pirate-spaces` | verification-only Spaces | `0acefe32b75e242d37a965f7f2eed6f2fdd5e9b3` | loopback-only, syncing, drift none |
| ns2 `/srv/pirate-hns-secondary` | secondary DNS | `44c23421cee7452e78c01afd8fda33f2c0d9175d` | running, zero zones, drift none |

Sync snapshot:

- Spaces on ns1: height `948081`; observed network tip `958544`.
- HNS observer: `130365/130365` downloaded headers/blocks,
  `verificationprogress=0.3832276739`. Equal local blocks/headers does **not**
  mean network-tip sync during IBD. Docker health intentionally remains red
  until the strict tip/progress/freshness gate passes.
- Local Spaces reference node: fully synced and ready at height `958544`.
  Wallet `default` owns `@pirate` at
  `d6b8d32ad304cd91a3e5bc6879e1e77e079e6ae3abe3733592cdec236730d58b:1`.
  The space is in the normal transfer covenant, expires at height `976904`,
  and currently has no delegation or commitment. No wallet mutation is queued.

## Resume checks

From ns1:

```bash
sudo journalctl -u pirate-spaced.service -n 3 --no-pager -o cat
curl -fsS http://127.0.0.1:4047/health
sudo docker inspect pirate-hsd-observer --format '{{.State.Health.Status}}'
sudo /srv/pirate-hns/current/bin/deployment-status.sh --deploy-root /srv/pirate-hns
sudo /srv/pirate-hns-authdns/current/bin/deployment-status.sh --deploy-root /srv/pirate-hns-authdns
sudo /srv/pirate-hns-observer/current/bin/deployment-status.sh --deploy-root /srv/pirate-hns-observer
sudo /srv/pirate-spaces/current/bin/deployment-status.sh --deploy-root /srv/pirate-spaces
```

From ns2:

```bash
sudo /srv/pirate-hns-secondary/current/bin/deployment-status.sh \
  --deploy-root /srv/pirate-hns-secondary
```

Local read-only Spaces checks:

```bash
cd ~/Downloads/spaces
CLI=./spaces-v0.1.1-linux-x86_64/space-cli
COOKIE=./tmp-spaced-data-0.1.1/mainnet/.cookie
$CLI --rpc-url http://127.0.0.1:7225 --rpc-cookie "$COOKIE" \
  --output-format json getserverinfo
$CLI --rpc-url http://127.0.0.1:7225 --rpc-cookie "$COOKIE" \
  --output-format json getspace @pirate
```

Do not export a wallet, publish, operate, commit, delegate, transfer, or renew
as part of resume inspection.

## Remaining work in order

1. **Owner console, before delegation:** regenerate/burn the Backblaze
   bootstrap/master key; the scoped uploader survives and cannot delete or
   weaken retention. Confirm in Fluence that both public IPv4 addresses are
   persistent before publishing glue.
2. **When Spaces reaches tip:** prove server readiness and inspect `@pirate`.
   Public TLS/DNS routing for `verifier.pirate.sc`, the production API verifier
   secret, and an inspection smoke test remain undone. The current public name
   must not be treated as live. A Fabric publish E2E may require an on-chain
   `operate`/commit action; review the exact transaction and fee before asking
   the owner to run it.
3. **When HNS observer health turns green:** deploy/activate the HNS verifier
   assertions and public gateway, create the first backed-up DNSSEC zone, and
   prove UDP and TCP 53 plus DNSSEC from independent external vantage points.
4. **Delegation ceremony:** only after both authorities and the real zone are
   proven, perform the required `.pirate` control proof and publish NS, glue,
   and DS. Validate end to end before merging Web #220. Enable namespace
   revalidation last.

## Explicitly not blockers

- The token embedded in the Chainstack HTTPS endpoint grants Bitcoin RPC/quota
  access, not Spaces wallet custody. Rotation is optional and not on the
  cutover critical path.
- HNS renewal-wallet design and mainnet renewal execution are outside this
  bring-up scope.
- Local wallet/log cleanup is hygiene, not a verifier/DNS deployment gate.

## Holds

- Web #220 remains last; do not merge it before public HNS resolution works.
- Do not publish HNS NS/glue/DS while either IP persistence is unconfirmed or
  external authoritative/DNSSEC validation is incomplete.
- Do not expose loopback RPC ports or place wallet material on either DNS host.
