# Path on the deployment host. Infisical should render a random value of at
# least 32 characters into this file with mode 0600 before compose starts.
HSD_API_KEY_FILE=./secrets/hsd_api_key
# UID/GID that own ./data on the deployment host.
HSD_UID=1000
HSD_GID=1000
