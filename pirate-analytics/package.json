{
  "name": "pirate-analytics",
  "private": true,
  "license": "AGPL-3.0-or-later",
  "type": "module",
  "scripts": {
    "dev": "bun run build && wrangler dev --local --port 8788",
    "build": "tsc -b && vite build",
    "preview": "wrangler dev --local --port 8788",
    "deploy": "bun run build && wrangler deploy"
  },
  "dependencies": {
    "react": "latest",
    "react-dom": "latest"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "latest",
    "@types/react": "latest",
    "@types/react-dom": "latest",
    "@vitejs/plugin-react": "latest",
    "typescript": "latest",
    "vite": "latest",
    "wrangler": "latest"
  }
}
